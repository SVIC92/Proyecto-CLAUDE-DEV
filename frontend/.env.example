# URL base de la API. En desarrollo se puede omitir: Vite usa un proxy interno
# (ver vite.config.js) que reenvía /api hacia http://localhost:3000.
# Para build de producción, apunta directamente al backend:
VITE_API_BASE_URL=http://localhost:3000
